namespace Libralgo.Models.Enums
{
    public enum LibraryMemberStatus
    {
        Pending = 1, // Onay Bekliyor
        Active = 2, // Erişim Verildi
        Revoked = 3 // Erişim Kaldırıldı
    }
}
